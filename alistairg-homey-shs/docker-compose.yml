version: "3.7"
services:
  app_proxy:
    environment:
      APP_HOST: 127.0.0.1
      APP_PORT: 80
      
  server:
    image: ghcr.io/athombv/homey-shs:latest
    restart: unless-stopped
    network_mode: host
    privileged: true
    volumes:
      - ${APP_DATA_DIR}/data:/homey/user