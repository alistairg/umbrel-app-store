version: "3.7"
services:
      
  server:
    image: ghcr.io/athombv/homey-shs:latest
    restart: unless-stopped
    network_mode: host
    privileged: true
    volumes:
      - ${APP_DATA_DIR}/data:/homey/user